# ── Core Framework
Django>=4.2,<5.0
asgiref>=3.7.0
sqlparse>=0.5.0

# ── Production WSGI / Web Server
gunicorn>=21.2.0

# ── Static Files Serving (WhiteNoise with Brotli/Gzip)
whitenoise[brotli]>=6.6.0

# ── Database (PostgreSQL + Connection Pooling + SQLite fallback)
dj-database-url>=2.1.0
psycopg2-binary>=2.9.9

# ── Environment Variables
python-dotenv>=1.0.0

# ── Image Processing
Pillow>=10.0.0

# ── Authentication & Security (Allauth v65+)
django-allauth>=65.0.0
PyJWT>=2.8.0
cryptography>=42.0.0

# ── Admin Interface
django-jazzmin>=3.0.0
django-unfold>=0.67.0

# ── Captcha & Security Headers
django-hCaptcha>=0.2.0
django-csp>=3.7.3

# ── Web Push Notifications
pywebpush>=1.14.0
py-vapid>=1.9.0

# ── HTTP Requests & Scraping
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── Date & Time Utilities
pytz>=2023.3
python-dateutil>=2.8.2

# ── Excel / Data Export
openpyxl>=3.1.0
